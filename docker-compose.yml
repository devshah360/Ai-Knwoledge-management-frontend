version: "3.9"

services:

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"

  backend:
    build: ./backend
    ports:
      - "8000:8000"

  elasticsearch:
    image: elasticsearch:8.14.0

  redis:
    image: redis:7

  mongodb:
    image: mongo:7